// function - can be used for dynamic data loading
// fetching the API - Integration
// Dynamic widgets - Integration

/// Classes & Objects - blue print
/// Dynamic Design Pattern - Software Level, Manufacturing level
/// Kia Sonet - Manufacturing: Design remains same for almost all cars
/// Animations, buttons, widgets, etc. - import class

// function vs method
// funtion will work independently
// method will work with the class

// print(), len(), all(), any(), max(), min(), sum(), count() - Python functions
// List.append(), clear(), copy(), extend(), insert(), pop(), remove(), reverse(), sort() - Python methods
// dict.update() - method

/**
   * Fields or arrtributes
   * Constructors
   * Methods
   * Getters and Setters
   * default size for class datatype - 1byte
   */

class ClassName {
  static String brand = "Apple"; // class attribute
  static String model = "iPhone 13"; // class attribute

  // Constructor - will execute when instantiation happens
  ClassName(String name) {
    print("$name gonna purchase a $brand $model");
  }
}

instance() {
  // Instance of class
  var praveen = ClassName("praveen"); // instance praveen - Instantiation
  var harish = ClassName("harish"); // instance harish - Instantiation
  var keerthi = ClassName("keerthi"); // instance keerthi - Instantiation
  var suresh = ClassName("suresh"); // instance suresh - Instantiation
}
